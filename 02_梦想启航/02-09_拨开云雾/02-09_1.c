#include <stdio.h>
#include <stdlib.h>
int main()
{
	int a,b,c;
    scanf("%d%d", &a, &b);
    // c = a + b;
    // printf("%d + %d = %d\n", a, b, c);
    printf("%d + %d = %d\n", a, b, a + b);
	system("pause");
	return 0;
}
