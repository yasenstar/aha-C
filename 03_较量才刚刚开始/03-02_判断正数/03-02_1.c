#include <stdio.h>
#include <stdlib.h>
int main()
{
	int a;
    scanf("%d", &a);
    // check whether it's positive integer 判断是否是正数
    if (a>0) {
        printf("yes\n");
    }
	system("pause");
	return 0;
}
